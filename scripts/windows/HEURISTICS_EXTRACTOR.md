# Heuristics extractor moved

Offline Cursor/Grok heuristic extraction + versioned skip-list live in:

`C:\GIT\projetos-paralelos\ai-memory_extractor`

```powershell
cd C:\GIT\projetos-paralelos\ai-memory_extractor
python scripts/extract_heuristics.py
python scripts/archive_cleanup.py
python scripts/skip_list.py stats
```

See that repo's `README.md` and `skip-list.json`.
